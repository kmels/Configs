#!/bin/bash

#invert Super_L and Alt_L
xmodmap -e 'keycode 133=0xffe9' &
xmodmap -e 'keycode  64=0xffeb' &

~/.cabal/bin/xmonad &

emacs ~/Dropbox/org/kmels.org &
google-chrome "mail.kmels.net" & #gmail shortcuts interfere with conkeror's
conkeror "reddit.com" & #the home page of the internet
clementine &
gnome-terminal &
bitlbee -c ~/.bitlbee.conf &
xchat & 
skype &